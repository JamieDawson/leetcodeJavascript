var smallerNumbersThanCurrent = function(nums) {
    //make hash

    //loop nums lenght
        //if the key exist, increase it by one.
        //else
            // create it and set it to one.

    //loop through 
};

let nums = [8,1,2,2,3];
smallerNumbersThanCurrent(nums);