const testArr = (arr, row, column) => {
  let queue = [[arr[row][column]]];
  
  while(queue.length > 0) {
    const [current] = queue.shift();


    
  }
};

const arr = [
  [1, 2, 3],
  [4, 5, 6],
  [7, 8, 9],
];

let row = 0;
let column = 0;

testArr(arr, row, column);
