var search = function(nums, target) {
    var middle = (nums.length - 1) / 2;
    console.log(Math.round(middle));
};

var nums = [4,5,6,7,0,1,2];
var target = 0;

search(nums, target);


/*
check right side:
if target is less than or equal to middle-1

*/