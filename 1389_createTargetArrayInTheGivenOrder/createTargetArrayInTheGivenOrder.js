var createTargetArray = function (nums, index) {
	var kv = {};

	for (var i = 0; i < index.length; i++) {
        if (kv[index[i]]) 
	}
   // console.log(index[i]); //get index item

	//console.log(kv);
};

let nums = [0, 1, 2, 3, 4];
let index = [0, 1, 2, 2, 1];

createTargetArray(nums, index);

/*
if kv index exist
    push value in it
else
    create index and add value.

*/
