var groupAnagrams = function (strs) {};

/*
Create a hash table

loop through each item in the array
    hold onto the sorted string
    if sorted string DOES NOT exist in hash table
        create it and push it
    else
        push it into the existing section

return hash table.

*/
